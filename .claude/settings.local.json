{
  "permissions": {
    "allow": [
      "Skill(update-config)",
      "Skill(rule-curation)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null) && jq -r '.tool_input.file_path' | { read -r f; if [[ \"$f\" == *.mdc ]]; then node \"$ROOT/scripts/validate-rules.mjs\" \"$f\"; fi; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Validating .mdc file..."
          }
        ]
      }
    ]
  }
}
