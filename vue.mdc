---
description: 适用于现代 Web 应用程序的 Vue.js 最佳实践和模式
globs: **/*.vue, **/*.ts, components/**/*
---

# Vue.js 最佳实践

## 组件结构
- 使用 `<script setup lang="ts">` 语法糖（组合式 API + TypeScript）
- 保持组件小而专注，单个组件不超过 200 行
- 使用 `defineProps<{ title: string; count?: number }>()` 定义带类型的 props
- 使用 `defineEmits<{ (e: 'update', id: number): void }>()` 声明事件
- 遵循 `<script>` → `<template>` → `<style>` 的代码块顺序
- 模板中避免复杂表达式，将逻辑移至 `computed` 或方法中

## 组合式 API
- 基本类型使用 `ref()`，对象/数组使用 `reactive()`
- 使用 `onMounted` 进行 DOM 操作，`onUnmounted` 清理定时器和事件监听
- 将可重用逻辑提取为 `composables/useXxx.ts` 组合式函数
- 在 `<script setup>` 中按逻辑关注点组织代码，而非按选项类型
- 使用 `computed()` 创建缓存的派生值，避免在模板中重复计算
- 使用 `watch()` 响应特定数据变化，`watchEffect()` 自动追踪依赖

## 状态管理
- 使用 Pinia 作为状态管理库，替代 Vuex
- 每个领域创建独立的 store（`stores/useUserStore.ts`, `stores/useCartStore.ts`）
- 使用 `defineStore` 的 Setup 语法（类似 `<script setup>`）定义 store
- 在 actions 中处理异步逻辑（API 调用），getters 用于派生状态
- 使用 `storeToRefs()` 解构 store 的响应式属性以保持响应性
- 对需要持久化的状态使用 `pinia-plugin-persistedstate`

## 性能
- 使用 `defineAsyncComponent(() => import('./HeavyComponent.vue'))` 懒加载组件
- 使用 `<KeepAlive>` 缓存频繁切换的组件状态
- 优先使用 `computed` 替代 `watch`，避免不必要的副作用
- 频繁切换显示的元素使用 `v-show`，条件性渲染使用 `v-if`
- 在 `v-for` 中始终提供唯一的 `:key`，且不要与 `v-if` 用在同一元素上
- 使用 `shallowRef()` / `shallowReactive()` 处理大型只读数据集

## 路由
- 使用 Vue Router 4.x 配合组合式 API（`useRoute()`, `useRouter()`）
- 使用 `beforeEach` 全局守卫实现认证检查和权限控制
- 使用路由 `meta` 字段存储页面标题、权限等元数据
- 使用 `props: true` 将路由参数作为 props 传入组件
- 使用 `() => import('./views/UserPage.vue')` 实现路由级代码分割
- 使用 `router.push({ name: 'user', params: { id } })` 命名路由导航

## 表单
- 使用 `v-model` 实现表单输入的双向绑定
- 使用 VeeValidate + Zod 或 Yup 进行声明式表单验证
- 使用 `ref` 追踪提交状态（`isSubmitting`, `errors`）
- 提交时禁用按钮并显示加载指示器
- 使用 `<ErrorMessage />` 组件展示字段级别的验证错误
- 提交成功后使用 `resetForm()` 重置表单状态

## TypeScript 集成
- 在 `env.d.ts` 中声明 `.vue` 文件的类型模块
- 使用泛型定义 props：`defineProps<Props>()` 而非 `defineProps({ ... })`
- 使用 `PropType<T>` 为复杂 prop 类型提供类型标注（选项式 API 场景）
- 组合式函数的返回值提供明确的类型标注
- 使用 `Ref<T>`, `ComputedRef<T>` 标注 ref 和 computed 的类型
- 在 Pinia store 中为 state 提供完整的类型定义

## 测试
- 使用 Vitest 作为单元测试框架（与 Vite 无缝集成）
- 使用 `@vue/test-utils` 的 `mount()` / `shallowMount()` 渲染组件
- 使用 `wrapper.find()`, `wrapper.trigger()` 测试用户交互
- 使用 `createTestingPinia()` 模拟 Pinia store
- 在独立环境中测试组合式函数的输入/输出
- 使用 Playwright 或 Cypress 进行端到端测试

## 构建和工具
- 使用 Vite 作为开发服务器和构建工具
- 使用 `import.meta.env.VITE_API_URL` 访问环境变量
- 利用 Vite 的自动代码分割，配合路由懒加载减小首屏体积
- 使用 `vite-plugin-vue-devtools` 提升开发调试体验
- 使用 `unplugin-auto-import` 和 `unplugin-vue-components` 自动导入
